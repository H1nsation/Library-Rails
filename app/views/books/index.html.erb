<h1 class="text-center">Catalogue</h1>
<table>
	<tr>
		<th>Title</th>
		<th>Quantity</th>
		<th>Details</th>
	</tr>
	<% @books.each do |book| %>
		<tr>
			<td> <%= book.title %> </td>
			<td> <%= book.quantity %> </td>
			<td> <%= link_to "view", book %> </td>
		</tr>
	<% end %>
</table>

